<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.charm.user">
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
                android:usesCleartextTraffic="true"
        tools:ignore="AllowBackup,UnusedAttribute">
        <activity android:name="com.charm.user.MainActivity"
            android:theme="@style/AppTheme"/>
        <activity
            android:name="com.charm.user.DialogForgotPassword"
            tools:ignore="Instantiatable"
            android:theme="@style/AppTheme"/>
        <activity
            android:name="com.charm.user.LoginActivity"
            android:windowSoftInputMode="stateHidden"
            android:theme="@style/Theme"/>
        <activity
            android:name="com.charm.user.EmployeeRegisterActivity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:theme="@style/Theme"
            android:windowSoftInputMode="stateHidden" />
        <activity android:name="com.charm.user.SplashActivity"
            android:theme="@style/AppTheme">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>