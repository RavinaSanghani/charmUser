<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowFullscreen">true</item>
    </style>
    <style name="EditTextTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorControlNormal">@color/themeTextUnselectedTintColor</item>
        <item name="colorControlActivated">@color/themeTextSelectedTinyColor</item>
        <item name="colorControlHighlight">@color/themeTextSelectedTinyColor</item>
    </style>
</resources>
